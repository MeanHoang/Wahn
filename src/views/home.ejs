<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<style>
    #customers {
        font-family: Arial, Helvetica, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

    #customers td,
    #customers th {
        border: 1px solid #ddd;
        padding: 8px;
    }

    #customers tr:nth-child(even) {
        background-color: #f2f2f2;
    }

    #customers tr:hover {
        background-color: #ddd;
    }

    #customers th {
        padding-top: 12px;
        padding-bottom: 12px;
        text-align: left;
        background-color: #04AA6D;
        color: white;
    }

    .table-container {
        padding: 15px 20px;
    }

    .title {
        font-size: 25px;
        font-weight: 600;
        padding-bottom: 10px;
    }
</style>

<body>
    <div class="topnav">
        <a class="active" href="/">Home</a>
        <a href="/create">Create user</a>
    </div>

    <div class="table-container">
        <div class="title">List Users</div>
        <table id="customers">
            <!-- <%= JSON.stringify(listUsers) %> -->
            <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Password</th>
                <th>Firstname</th>
                <th>Surname</th>
                <th>Email</th>
                <th>Address</th>
                <th>Action</th>
            </tr>

            <!-- <% for (let i=0; i < listUsers.length; i++) { %>
                <tr>
                    <td>
                        <%=listUsers[i].user_id %>
                    </td>
                    <td>
                        <%=listUsers[i]. user_username %>
                    </td>
                    <td>
                        <%=listUsers[i].user_password %>
                    </td>
                    <td>
                        <%=listUsers[i].user_firstname %>
                    </td>
                    <td>
                        <%=listUsers[i].user_surname %>
                    </td>
                    <td>
                        <%=listUsers[i].user_email %>
                    </td>
                    <td>
                        <%=listUsers[i].user_address %>
                    </td>
                    <td>
                        <button>Edit</button>
                        <button>Delete</button>
                    </td>
                </tr>
                <% } %> -->

            <ul>
                <% listUsers.forEach(function(user, index) { %>
                    <tr>
                        <td>
                            <%= user.user_id %>
                        </td>
                        <td>
                            <%= user.user_username %>
                        </td>
                        <td>
                            <%= user.user_password %>
                        </td>
                        <td>
                            <%= user.user_firstname %>
                        </td>
                        <td>
                            <%= user.user_surname %>
                        </td>
                        <td>
                            <%= user.user_email %>
                        </td>
                        <td>
                            <%=user.user_address %>
                        </td>
                        <td>
                            <a href="/update/<%= user.user_id %>">Edit</a>
                            <button>Delete</button>
                        </td>
                    </tr>
                    <% }); %>
            </ul>


        </table>
    </div>

</body>

</html>